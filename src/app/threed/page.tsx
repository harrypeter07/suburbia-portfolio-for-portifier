"use client";
import React from "react";

const ThreeDPage = () => {
	return (
		<section className="flex flex-col items-center justify-center min-h-screen bg-white gap-12 py-12">
			{/* 3D content removed as per user request */}
			<div className="text-xl text-zinc-600 font-semibold">
				3D section is currently empty.
			</div>
		</section>
	);
};

export default ThreeDPage;
